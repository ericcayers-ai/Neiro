{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Main Neiro window — local engine only",
  "windows": ["main"],
  "permissions": ["core:default"]
}
